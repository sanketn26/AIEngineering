# Agentic plugin starter — milestone TODOs

- [x] Day 1: one command + mock model + `read_file` allowlist (`tests/test_slice.py`)
- [ ] IPC envelope (`id`, `method`, `params`, `error`) so a TS extension can be thin
- [ ] `max_steps` + repeated-args abort on the agent loop
- [ ] Write tools (`apply_diff`) default **deny** until human approval
- [ ] Worktree + merge gate (source tree unchanged until merge)
- [ ] Trajectory eval in CI; prompt/tool-list pin (Modules 22–23)
- [ ] Secrets only in SecretStorage / env — never settings.json

Track rubric: [docs/reference/assessment.md](../../../docs/reference/assessment.md).
